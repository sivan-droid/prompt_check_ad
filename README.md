# Prompt Test Agent

This agent validates prompts using an LLM (Groq LLaMA).

## Categories Tested
- Good Prompt
- Harsh Prompt (toxic language)
- Harmful Prompt (illegal / unsafe)

## Run

```bash
pip install -r requirements.txt
python main.py