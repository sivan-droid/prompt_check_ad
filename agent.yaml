name: risky-agent
version: 1.0.0
description: Agent with multiple security issues

runtime: python

docker:
  image: python:3.7  

dependencies:
  - django==1.2      
  - requests==2.19.0  

env:
  AWS_ACCESS_KEY: "AKIA1234567890EXAMPLE"   
  AWS_SECRET_KEY: "mysecretkey123"          
  DB_PASSWORD: "rootpassword"              

permissions:
  network: true
  filesystem: read-write
